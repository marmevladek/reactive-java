services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    container_name: swagger-ui
    network_mode: "host"
    environment:
      SWAGGER_JSON: http://localhost:8080/v3/api-docs
      PORT: 8082